# Novi 仓库工作要求

## 每次任务开始前

1. 在分析、修改代码或运行验证命令之前，必须完整阅读 `docs/IMPLEMENTATION_PROGRESS.md`。
2. 根据进度文档确认当前已完成能力、未完成事项、外部门禁和最近已知问题，不得重复宣称已完成的工作，也不得把尚未取得证据的事项写成已完成。
3. 如果本次任务与进度文档记录不一致，应先以仓库代码、测试结果和可复现证据核对真实状态。

## 每次任务结束前

1. 如果实现状态、已知问题、验证结果或下一步发生变化，必须同步更新 `docs/IMPLEMENTATION_PROGRESS.md`。
2. 只有在实现完成且相关验证通过后，才能把事项移动到“已完成”；依赖真实供应商、目标基础设施、签名证书或人工验收的事项必须保留在“未完成”或“外部门禁”。
3. 更新进度时记录日期、验证命令或证据，以及仍然存在的限制。

## 提交与推送边界

1. 每完成一个可独立验收的完整功能或完整修复，必须在开始下一项功能前完成相关验证、更新 `docs/IMPLEMENTATION_PROGRESS.md`、创建 Git 提交并推送至当前远程分支。
2. 一个提交只包含该功能/修复及其测试、契约和文档；不得把无法独立验收的脚手架或多个无关功能混入同一完成提交。
3. 如果推送失败，先保留已验证的本地提交并解决推送问题或明确报告阻塞，不得在未说明状态的情况下继续堆叠后续功能。

## 文档一致性

- 项目运行方法和常见启动故障维护在 `README.md`。
- 正式发布步骤维护在 `docs/RELEASE.md`。
- 商用发布证据和外部门禁维护在 `docs/COMMERCIAL_READINESS.md`。
- 原始目标逐项映射维护在 `docs/GOAL_TRACEABILITY.md`。
